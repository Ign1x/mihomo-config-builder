proxies:
  - name: DEMO-HK
    type: ss
    server: hk.demo.example
    port: 443
    cipher: aes-128-gcm
    password: demo-password
proxy-groups:
  - name: PROXY
    type: select
    proxies:
      - DEMO-HK
      - DIRECT
rules:
  - MATCH,PROXY
dns:
  enable: true
  enhanced-mode: fake-ip
  fake-ip-filter:
    - +.lan
